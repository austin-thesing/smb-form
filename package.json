{
  "name": "smb-form",
  "version": "1.0.0",
  "description": "SMB Form with HubSpot integration",
  "main": "ms-form-route.js",
  "scripts": {
    "build": "cross-env NODE_ENV=production rimraf dist && esbuild ms-form-route.js --bundle --minify --sourcemap --target=es2015 --outfile=dist/ms-form-route.js",
    "watch": "esbuild ms-form-route.js --bundle --sourcemap --target=es2015 --outfile=dist/ms-form-route.js --watch"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "esbuild": "^0.20.1",
    "nodemon": "^3.1.0",
    "rimraf": "^5.0.5"
  }
}
