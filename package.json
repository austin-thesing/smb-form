{
  "name": "forms-builder",
  "version": "1.0.0",
  "description": "SMB Form with HubSpot integration",
  "main": "src/core/ms-form-route.js",
  "scripts": {
    "build": "cross-env NODE_ENV=development node build.js",
    "build:prod": "cross-env NODE_ENV=production node build.js",
    "watch": "nodemon --watch src --ext js,json --exec \"npm run build\""
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "esbuild": "^0.20.1",
    "nodemon": "^3.1.0",
    "prettier": "^3.5.3",
    "rimraf": "^5.0.5"
  }
}
